% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{srf_model}
\alias{srf_model}
\title{Create a spatial random field model}
\usage{
srf_model(nug, spatial = mdgm(), emission = NULL, n_colors = 2L)
}
\arguments{
\item{nug}{A \code{NaturalUndirectedGraph} object defining the spatial structure.}

\item{spatial}{A spatial configuration object created by \code{\link[=mdgm]{mdgm()}} or
\code{\link[=mrf]{mrf()}}.}

\item{emission}{Optional emission family for hierarchical models:
\code{"bernoulli"}, \code{"gaussian"}, or \code{"poisson"}. If \code{NULL} (default),
creates a standalone model where the spatial field is observed directly.}

\item{n_colors}{Number of categories for the spatial field (default 2).}
}
\value{
An \link{SrfModel} object.
}
\description{
Constructs a spatial random field model for MCMC inference. The spatial
component can be either a mixture of directed graphical models (MDGM) or
a Markov random field (MRF), specified via the \code{spatial} argument using
the \code{\link[=mdgm]{mdgm()}} or \code{\link[=mrf]{mrf()}} configuration helpers.
}
\examples{
edges <- rbind(c(1, 2), c(2, 1), c(2, 3), c(3, 2), c(1, 3), c(3, 1))
nug <- nug_from_edge_list(3, edges, seed = 42L)

# MDGM model
m1 <- srf_model(nug, spatial = mdgm(dag_type = "spanning_tree"))

# MRF model with exchange algorithm
m2 <- srf_model(nug, spatial = mrf(method = "exchange"),
                emission = "bernoulli")
}
