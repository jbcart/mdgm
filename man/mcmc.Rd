% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmc}
\alias{mcmc}
\title{Run MCMC inference for an MDGM model}
\usage{
mcmc(
  model,
  y = NULL,
  z_init,
  psi_init,
  eta_init = numeric(0),
  n_iter = 1000L,
  psi_tune = 0.1,
  emission_prior_params = c(1, 1),
  seed = NULL
)
}
\arguments{
\item{model}{An \link{MdgmModel} object created by \code{\link[=mdgm_model]{mdgm_model()}}.}

\item{y}{Observation data. For hierarchical models, a list of integer
vectors where \code{y[[i]]} contains the observations for vertex \code{i}.
Vertices with no observations should have \code{integer(0)}. For standalone
models, pass \code{NULL} (default).}

\item{z_init}{Initial color assignment, an integer vector of length \code{n}
with values in \verb{0, ..., n_colors - 1}.}

\item{psi_init}{Initial value for the dependence parameter (positive).}

\item{eta_init}{Initial emission parameters. For Bernoulli, a numeric
vector of length \code{n_colors} (e.g., \code{c(0.2, 0.8)}). Ignored for
standalone models, but should still be provided as \code{numeric(0)} or
a placeholder.}

\item{n_iter}{Number of MCMC iterations (default 1000).}

\item{psi_tune}{Standard deviation of the normal MH proposal for psi
(default 0.1).}

\item{emission_prior_params}{Prior hyperparameters for emission
parameters. For Bernoulli: \code{c(a, b)} for \code{Beta(a, b)} prior
(default \code{c(1, 1)}).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An \link{MdgmResult} object containing posterior samples.
}
\description{
Performs Markov chain Monte Carlo sampling to estimate the posterior
distribution of the spatial field, DAG structure, dependence parameter,
and (for hierarchical models) emission parameters.
}
\examples{
\dontrun{
# Create a small grid graph
A <- matrix(0, 4, 4)
A[1, 2] <- A[2, 1] <- A[2, 3] <- A[3, 2] <- 1
A[3, 4] <- A[4, 3] <- 1
nug <- nug_from_adj_mat(A, seed = 42L)

# Standalone model
model <- mdgm_model(nug, dag_type = "spanning_tree")
result <- mcmc(model, z_init = c(0L, 0L, 1L, 1L),
               psi_init = 0.5, n_iter = 100L)
result$summary()
}
}
\seealso{
\code{\link[=mdgm_model]{mdgm_model()}} for model construction, \link{MdgmResult} for
accessing results.
}
