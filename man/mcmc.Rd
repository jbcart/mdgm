% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmc}
\alias{mcmc}
\title{Run MCMC inference for a spatial random field model}
\usage{
mcmc(
  model,
  y = NULL,
  z_init,
  psi_init,
  theta_init = numeric(0),
  n_iter = 1000L,
  psi_tune = 0.1,
  emission_prior_params = NULL,
  store_z = FALSE,
  seed = NULL,
  nug = NULL
)
}
\arguments{
\item{model}{An \link{SrfModel} object created by \code{\link[=srf_model]{srf_model()}}.}

\item{y}{Observation data. For hierarchical models, a list of numeric
vectors where \code{y[[i]]} contains the observations for vertex \code{i}.
For Bernoulli, values should be 0 or 1; for Poisson, non-negative
integers; for Gaussian, any real values. Vertices with no observations
should have \code{numeric(0)}. For standalone models, pass \code{NULL} (default).}

\item{z_init}{Initial color assignment, an integer vector of length \code{n}
with values in \verb{0, ..., n_colors - 1}.}

\item{psi_init}{Initial value for the dependence parameter (positive).}

\item{theta_init}{Initial emission parameters. For Bernoulli, a numeric
vector of length \code{n_colors} (e.g., \code{c(0.2, 0.8)}). For Gaussian,
a vector of length \code{2 * n_colors} with means then variances
(e.g., \code{c(mu_1, mu_2, sigma2_1, sigma2_2)}). For Poisson, a vector of
length \code{n_colors} with rate parameters. Ignored for standalone models.}

\item{n_iter}{Number of MCMC iterations (default 1000).}

\item{psi_tune}{Standard deviation of the normal MH proposal for psi
(default 0.1).}

\item{emission_prior_params}{Prior hyperparameters for emission
parameters. For Bernoulli: \code{c(a, b)} for \code{Beta(a, b)} prior.
For Gaussian: \code{c(mu_0, sigma2_0, alpha_0, beta_0)} where
\code{mu_k ~ N(mu_0, sigma2_0)} and \code{sigma_k^2 ~ InvGamma(alpha_0, beta_0)}
independently. For Poisson: \code{c(alpha_0, beta_0)} for
\code{Gamma(alpha_0, beta_0)} prior. Default: \code{c(1, 1)} for Bernoulli/Poisson,
\code{c(0, 10000, 0.01, 0.01)} for Gaussian (non-informative).}

\item{store_z}{Logical; if \code{TRUE}, store the full latent field matrix
(\verb{n x n_iter}). Default is \code{FALSE} to conserve memory --- on large grids
(e.g. 1000x1000 with 10,000 iterations), the z matrix alone requires
~40 GB. When \code{FALSE}, per-iteration summary statistics (allocation
counts, sufficient statistics, MAP configuration) are still computed.}

\item{seed}{Optional integer seed for reproducibility.}

\item{nug}{Optional \code{NaturalUndirectedGraph} object. If provided, stored
in the result for use by \code{edge_inclusion_probs()} and \code{plot()}.}
}
\value{
An \link{SrfResult} object containing posterior samples.
}
\description{
Performs Markov chain Monte Carlo sampling to estimate the posterior
distribution of the spatial field, dependence parameter, and (for
hierarchical models) emission parameters. For MDGM models, the DAG
structure is also sampled.
}
\examples{
\dontrun{
# Create a small grid graph
A <- matrix(0, 4, 4)
A[1, 2] <- A[2, 1] <- A[2, 3] <- A[3, 2] <- 1
A[3, 4] <- A[4, 3] <- 1
nug <- nug_from_adj_mat(A, seed = 42L)

# Standalone model
model <- srf_model(nug, spatial = mdgm(dag_type = "spanning_tree"))
result <- mcmc(model, z_init = c(0L, 0L, 1L, 1L),
               psi_init = 0.5, n_iter = 100L)
result$summary()
}
}
\seealso{
\code{\link[=srf_model]{srf_model()}} for model construction, \link{SrfResult} for
accessing results.
}
