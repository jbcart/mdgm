% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{mdgm_model}
\alias{mdgm_model}
\title{Create an MDGM model}
\usage{
mdgm_model(
  nug,
  dag_type = c("spanning_tree", "acyclic_orientation", "rooted"),
  n_colors = 2L,
  emission = NULL
)
}
\arguments{
\item{nug}{A \code{NaturalUndirectedGraph} object defining the spatial structure.}

\item{dag_type}{DAG construction type: \code{"spanning_tree"},
\code{"acyclic_orientation"}, or \code{"rooted"}.}

\item{n_colors}{Number of categories for the spatial field (default 2).}

\item{emission}{Optional emission family for hierarchical models:
\code{"bernoulli"}. If \code{NULL} (default), creates a standalone model where
the spatial field is observed directly.}
}
\value{
An \link{MdgmModel} object.
}
\description{
Constructs a mixture of directed graphical models for a discrete spatial
random field defined on an undirected graph.
}
\examples{
# Standalone model on a triangle graph
edges <- rbind(c(1, 2), c(2, 1), c(2, 3), c(3, 2), c(1, 3), c(3, 1))
nug <- nug_from_edge_list(3, edges, seed = 42L)
model <- mdgm_model(nug, dag_type = "spanning_tree")
model$nvertices()
model$has_emission()
}
