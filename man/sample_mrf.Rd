% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_mrf.R
\name{sample_mrf}
\alias{sample_mrf}
\title{Sample from a Markov Random Field}
\usage{
sample_mrf(
  nug,
  psi,
  n_colors = 2L,
  method = "auto",
  n_sweeps = 200L,
  seed = NULL
)
}
\arguments{
\item{nug}{A \code{NaturalUndirectedGraph} object (external pointer).}

\item{psi}{Spatial dependence parameter (positive real).}

\item{n_colors}{Number of colors/categories (default 2, binary).}

\item{method}{Sampling method: \code{"auto"} (CFTP for k=2, Swendsen-Wang for
k>2), \code{"swendsen_wang"}, \code{"gibbs"}, or \code{"cftp"} (k=2 only).}

\item{n_sweeps}{Number of sweeps (CFTP block size for binary, SW/Gibbs
sweeps for multi-color). Default 200.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
Integer vector of length \code{nvertices(nug)} with values in
\verb{0, 1, ..., n_colors - 1}.
}
\description{
Draw a single sample from a binary (Ising) or multi-color (Potts) MRF on a
given graph.
}
